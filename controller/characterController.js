exports.getAllCharacters = (req, res) => {
  res
    .status(200)
    .json({ status: "Success", message: "Nothing yet from character" });
};

exports.addCharacter = (req, res) => {
  res
    .status(200)
    .json({ status: "Success", message: "Nothing yet from character" });
};
